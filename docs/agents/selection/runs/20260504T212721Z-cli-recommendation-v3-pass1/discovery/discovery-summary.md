# Discovery Summary

Run id: `20260504T212721Z-cli-recommendation-v3-pass1`

This pass nominated three non-onboarded CLI candidates from public discovery evidence relevant to the fixed query family and excluded the currently onboarded ids (`aider`, `claude_code`, `codex`, `gemini_cli`, `opencode`).

- Candidate id `goose`, display_name `Goose`: query-family discovery surfaced current Goose CLI documentation, with official install docs, an active GitHub repository, and a public package registry listing.
- Candidate id `openhands`, display_name `OpenHands`: query-family discovery surfaced the OpenHands CLI landing page, with current CLI installation docs, a dedicated CLI GitHub repository, and a public package registry listing.
- Candidate id `gptme`, display_name `gptme`: query-family discovery surfaced the terminal-first gptme homepage, with getting-started docs, an active GitHub repository, and a public package registry listing.
